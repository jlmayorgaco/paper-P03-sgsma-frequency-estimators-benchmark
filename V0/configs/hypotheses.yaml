hypotheses:
  - id: H_EXAMPLE_01
    title: ZCD vs IPDFT RMSE on selected runs
    metric: m1_rmse_hz
    group_a: estimator:ZCD
    group_b: estimator:IPDFT
    test: mw
    alpha: 0.05
    correction: holm
    mode: preregistered

